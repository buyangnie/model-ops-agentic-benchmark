id: fo-ticket-intake-2round
name: FO Copilot 工单接入短测
fixture: fo-ticket-lifecycle
rounds:
  - "你是 FO Copilot。本轮最多 5 次工具调用，只按顺序调用这些工具：1 ticket_create_from_request(requestId=REQ-2001)；2 ticket_enrich(ticketId=INC-1007)；3 ticket_add_comment(ticketId=INC-1007, commentType=triage_summary)。然后用一句中文说明已创建的工单号。"
  - "继续处理 INC-1007。本轮最多 5 次工具调用，只按顺序调用这些工具：1 people_list_candidates(ticketId=INC-1007)；2 ticket_assign(ticketId=INC-1007, assigneeId=u-payment)；3 ticket_add_comment(ticketId=INC-1007, commentType=assignment_note)；4 ticket_validate_state(ticketId=INC-1007, expectedState=queued)。然后用一句中文总结。"
success:
  expectedTickets:
    - ticketId: INC-1007
      status: queued
      assigneeId: u-payment
  expectedComments:
    - ticketId: INC-1007
      commentType: triage_summary
    - ticketId: INC-1007
      commentType: assignment_note
